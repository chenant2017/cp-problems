#include <bits/stdc++.h>
#include <cmath>

#define ll long long
#define MAXN 1010

using namespace st

ll N, M, K;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	
	string fname = "cruise";
	freopen((fname + ".in").c_str(), "r", stdin);
	freopen((fname + ".out").c_str(), "w", stdout);
	
	cin >> N >> M >> K;

	for (ll i = 0; i < N; i++) {

	}

	return 0;
}